---
# Blank secret.yaml file
# Fill in credentials, then rename and encrypt with ansible-vault encrypt
- brassgoggles:
    userid:
    passwd:
    host:
    port:

    email:
        smtp_host:
        smtp_port:
        smtp_user:
        smtp_pass:
        email_to: []
